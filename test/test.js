const { expect } = require("chai");
const { ethers } = require("hardhat");

const bytes32 = require('bytes32');

const baseSize = 1000000;

describe("Bunga.live ", function () {
  it("bad news", async function () {
  });
});
